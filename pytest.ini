[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short
markers =
    slow: OCR / görüntü işleme gerektiren uçtan uca testler (varsayılan olarak atlanır, `-m slow` ile koşulur)
